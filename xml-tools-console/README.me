# xml-tools-console

Console classes to check xml-utils:

* XPathTester supports the following argument:
    - -xpath required states the XPath expression
    - -file required states the source XML file
    - -nameSpaceAware optional true/false states when check also for namespaces. If true a fake namespace is used for the default one 
